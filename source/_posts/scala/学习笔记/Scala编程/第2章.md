---
title: 第二章
categories: Scala
tags: 博客
---

[toc]


代码实例
```
args.foreach(arg => println(arg))
指定元素类型的写法，上述是简写
args.foreach((arg: String) => println(arg))

如果函数字面量只是一个接收单个参数的语句，可以不必给出参数名和参数本身，最终可以简写成：
args.foreach(println)
```



```
for (arg <- args)

arg是val的 不可以在循环中赋值
```

